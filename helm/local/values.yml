# Cluster specific local overrides
selfManage:
  enabled: true
  git:
    enabled: true
    repoURL: https://github.com/YOUR_ORG/argocd-deploy.git
    targetRevision: main
    path: charts/argocd-deploy
    valueFiles:
      - ../../local/values-homelab.yaml
  helm:
    enabled: false
  app:
    name: argocd
    project: default
